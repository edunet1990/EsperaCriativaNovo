
@model EsperaCriativa.ViewModels.Planejamentos.FormulariosExperienciasViewModel
@{
    Layout = "~/Views/Shared/_Layout.cshtml";
}
<link href="~/Content/Styles.css" rel="stylesheet" />
@{ var contFormulario = 0;
    var guardaFormularioID = 0;
    var contFormularioProButtonCriarNovo = 0;
    var contFormTb = 0;
    var usuarioId = Model.UsuarioId;

    foreach (var item in Model.FormulariosExperienciasVM)
    {
        if (item.UsuarioId == usuarioId)
        {
            foreach (var item2 in ViewBag.UsuarioFormularioTeste)
            {

                if (contFormTb < 2)
                {
                    contFormularioProButtonCriarNovo++;
                    contFormTb++;
                }
            }
        }
    }
}

            

@ViewBag.coultFormm

<!-- Content Wrapper. Contains page content -->
<div class="content-wrapper">
    <!-- Content Header (Page header) -->
    <section class="content-header" style="padding-bottom:50px">
        <h1>
            Seus planejamentos
            <small>PAD 0 - 1 e 6 meses</small>
        </h1>


        @if (contFormularioProButtonCriarNovo >= 2)
        {
            <button type="button" data-toggle="modal" data-target="#modal-alerta-mais-de-um-form" class="btn btn-md  botao-amarelo" style="float:right"> Criar Novo</button>
            <div class="modal  fade" id="modal-alerta-mais-de-um-form">
                <div class="modal-dialog ">
                    <div class="modal-content alerta-dois-botoes">
                        <div class="modal-header">
                            <h3>Atenção!</h3>
                        </div>
                        <div class="modal-body">
                            <h4>Há um limite de dois planejamentos por vez, volte a criar novos assim que houver algum insight!</h4>
                        </div>
                    </div>
                    <!-- /.modal-content -->
                </div>
                <!-- /.modal-dialog -->
            </div>
        }
        else
        {
            <ol class="breadcrumb">
                <li><button type="button" class="btn btn-md btn-primary  botao-amarelo" style="float:right"> @Html.ActionLink("Criar Novo", "Create", "Formularios", new { controller = "Formulario" })</button></li>
            </ol>
        }


    </section>
    <!-- Main content -->
    <section class="content">
        @if (contFormTb == 0)
        {
                <h1 style="text-align:center">Você não tem nenhum planejamento!</h1>
        }
        @*referência a contagem pra ver quantos formulários foram efetuados os insighs*@


        @{
            var countIdFormsDeComoFoi2 = 0;

            List<string> ListExperiencias = new List<string>();
            List<string> ListTituloExperiencias = new List<string>();
        }

        @foreach (var item in Model.FormulariosExperienciasVM)
        {


            if (!(contFormulario >= 2))
            {
                //verifiac se há na FK do comofoiaexperiencia um mesmo id do form
                for (int i = 0; i < Model.FormIdComoFoiAExper.Count; i++)
                {
                    if (item.FormularioID == Model.FormIdComoFoiAExper[i] && countIdFormsDeComoFoi2 < Model.FormIdComoFoiAExper.Count - 1)
                    {

                        countIdFormsDeComoFoi2++;
                    }
                }

                if (item.UsuarioId == ViewBag.UsuarioID)
                {
                    //var contadorId = 0;
                    <!-- Default box -->
                <div class="box">
                    <div class="box-header with-border">
                        <h3 class="box-title">@Html.DisplayFor(modelItem => item.Data)</h3>
                        <div class="box-tools pull-right">
                            <button type="button" class="btn btn-box-tool" data-widget="collapse" data-toggle="tooltip"
                                    title="Collapse">
                                <i class="fa fa-minus"></i>
                            </button>
                            <button type="button" class="btn btn-box-tool" data-widget="remove" data-toggle="tooltip" title="Remove">
                                <i class="fa fa-times"></i>
                            </button>
                        </div>
                    </div>
                    <div class="box-body">
                        <div class="row">
                            <div class="col-md-12">
                                <div class="box box-solid">
                                    <div class="box-header with-border">
                                        <h3 class="box-title"> @Html.DisplayFor(modelItem => item.Informacao)</h3>
                                        @*Guardar ID do Formulario*@
                                        @{ guardaFormularioID = @item.FormularioID;}
                                    </div>
                                    <!-- /.box-header -->
                                    <div class="box-body">
                                        @*Variável para controlar os títulos da experiência*@
                                        @{var naoRepetir = 0;
                                            var contTitulo = 0;
                                            int[] verificaQualEOTituloDosObjetivos = new int[] { 1, 2, 3, 4, 5 };
                                            int NumQCorespONomeDoTitul = 0;
                                        }
                                        <div class="box-group" id="accordion">
                                            @for (int i = 0; i < Model.ExperienciasFormulariosVM.Count; i++)
                                            {

                                                if (Model.ExperienciasFormulariosVM[i].FormularioID == item.FormularioID)
                                                {
                                                    if (Model.ExperienciasFormulariosVM[i].Checked)
                                                    {

                                                        if (Model.ExperienciasFormulariosVM[i].CheckBoxID <= 6)
                                                        {
                                                            if (naoRepetir == 6)
                                                            {
                                                                ViewBag.TituloExperiencias = "";
                                                                contTitulo = 0;
                                                            }
                                                            else
                                                            {
                                                                ViewBag.TituloExperiencias = "O EU, O OUTRO E O NÓS";
                                                                naoRepetir = 6;
                                                                NumQCorespONomeDoTitul = 1;
                                                            }

                                                        }
                                                        else if (Model.ExperienciasFormulariosVM[i].CheckBoxID >= 7 & Model.ExperienciasFormulariosVM[i].CheckBoxID <= 11)
                                                        {
                                                            if (naoRepetir == 11)
                                                            {
                                                                ViewBag.TituloExperiencias = "";
                                                            }
                                                            else
                                                            {
                                                                ViewBag.TituloExperiencias = "CORPO, GESTOS E MOVIMENTOS";
                                                                naoRepetir = 11;
                                                                NumQCorespONomeDoTitul = 2;
                                                            }

                                                        }
                                                        else if (Model.ExperienciasFormulariosVM[i].CheckBoxID >= 12 & Model.ExperienciasFormulariosVM[i].CheckBoxID <= 14)
                                                        {
                                                            if (naoRepetir == 24)
                                                            {
                                                                ViewBag.TituloExperiencias = "";
                                                            }
                                                            else
                                                            {
                                                                ViewBag.TituloExperiencias = "TRAÇOS, SONS, CORES E FORMAS";
                                                                naoRepetir = 24;
                                                                NumQCorespONomeDoTitul = 3;
                                                            }

                                                        }
                                                        else if (Model.ExperienciasFormulariosVM[i].CheckBoxID >= 15 & Model.ExperienciasFormulariosVM[i].CheckBoxID <= 23)
                                                        {
                                                            if (naoRepetir == 23)
                                                            {
                                                                ViewBag.TituloExperiencias = "";
                                                            }
                                                            else
                                                            {
                                                                ViewBag.TituloExperiencias = "ESCUTA, FALA, PENSAMENTO E IMAGINAÇÃO";
                                                                naoRepetir = 23;
                                                                NumQCorespONomeDoTitul = 4;
                                                            }


                                                        }
                                                        else if (Model.ExperienciasFormulariosVM[i].CheckBoxID >= 24 & Model.ExperienciasFormulariosVM[i].CheckBoxID <= 33)
                                                        {
                                                            if (naoRepetir == 29)
                                                            {
                                                                ViewBag.TituloExperiencias = "";
                                                                contTitulo = 30;
                                                            }
                                                            else
                                                            {
                                                                ViewBag.TituloExperiencias = "ESPAÇOS, TEMPOS, QUANTIDADES";
                                                                naoRepetir = 29;
                                                                NumQCorespONomeDoTitul = 5;
                                                            }

                                                        }

                                                        string teste22 = Model.ExperienciasFormulariosVM[i].Name.ToString();
                                                        ListTituloExperiencias.Add(ViewBag.TituloExperiencias);

                                                        if (ViewBag.TituloExperiencias == "O EU, O OUTRO E O NÓS")
                                                        {

                                                            <div class="panel ">
                                                                <div class="box-header ">
                                                                    <h4 class="box-title">
                                                                        <a data-toggle="collapse" data-parent="#accordion" href="#@i" style="color:black">
                                                                            @ViewBag.TituloExperiencias
                                                                        </a>
                                                                    </h4>
                                                                </div>


                                                                @for (int y = 0; y < Model.ExperienciasFormulariosVM.Count; y++)
                                                                {

                                                                    if (Model.ExperienciasFormulariosVM[y].FormularioID == item.FormularioID)
                                                                    {
                                                                        if (Model.ExperienciasFormulariosVM[y].Checked)
                                                                        {
                                                                            if (Model.ExperienciasFormulariosVM[y].CheckBoxID <= 6)
                                                                            {
                                                                                ListExperiencias.Add(@Model.ExperienciasFormulariosVM[y].Name);

                                                                            }
                                                                        }
                                                                    }
                                                                }


                                                                <div id="@i" class="panel-collapse collapse">
                                                                    <div class="box-body">
                                                                        <ul>
                                                                            @foreach (var item2 in ListExperiencias)
                                                                            {
                                                                                <li> <div>@item2</div></li>

                                                                            }
                                                                        </ul>
                                                                    </div>
                                                                </div>

                                                            </div>
                                                            ListExperiencias.Clear();
                                                        }

                                                        if (ViewBag.TituloExperiencias == "CORPO, GESTOS E MOVIMENTOS")
                                                        {

                                                            <div class="panel ">
                                                                <div class="box-header ">
                                                                    <h4 class="box-title">
                                                                        <a data-toggle="collapse" data-parent="#accordion" href="#@i" style="color:black">
                                                                            @ViewBag.TituloExperiencias
                                                                        </a>
                                                                    </h4>
                                                                </div>


                                                                @for (int y = 0; y < Model.ExperienciasFormulariosVM.Count; y++)
                                                                {

                                                                    if (Model.ExperienciasFormulariosVM[y].FormularioID == item.FormularioID)
                                                                    {
                                                                        if (Model.ExperienciasFormulariosVM[y].Checked)
                                                                        {
                                                                            if (Model.ExperienciasFormulariosVM[y].CheckBoxID >= 7 & Model.ExperienciasFormulariosVM[y].CheckBoxID <= 11)
                                                                            {
                                                                                ListExperiencias.Add(@Model.ExperienciasFormulariosVM[y].Name);

                                                                            }
                                                                        }
                                                                    }
                                                                }


                                                                <div id="@i" class="panel-collapse collapse">
                                                                    <div class="box-body">
                                                                        <ul>
                                                                            @foreach (var item2 in ListExperiencias)
                                                                            {
                                                                                <li> <div>@item2</div></li>

                                                                            }
                                                                        </ul>
                                                                    </div>
                                                                </div>

                                                            </div>
                                                            ListExperiencias.Clear();
                                                        }

                                                        if (ViewBag.TituloExperiencias == "TRAÇOS, SONS, CORES E FORMAS")
                                                        {

                                                            <div class="panel ">
                                                                <div class="box-header ">
                                                                    <h4 class="box-title">
                                                                        <a data-toggle="collapse" data-parent="#accordion" href="#@i" style="color:black">
                                                                            @ViewBag.TituloExperiencias
                                                                        </a>
                                                                    </h4>
                                                                </div>


                                                                @for (int y = 0; y < Model.ExperienciasFormulariosVM.Count; y++)
                                                                {

                                                                    if (Model.ExperienciasFormulariosVM[y].FormularioID == item.FormularioID)
                                                                    {
                                                                        if (Model.ExperienciasFormulariosVM[y].Checked)
                                                                        {
                                                                            if (Model.ExperienciasFormulariosVM[y].CheckBoxID >= 12 & Model.ExperienciasFormulariosVM[y].CheckBoxID <= 14)
                                                                            {
                                                                                ListExperiencias.Add(@Model.ExperienciasFormulariosVM[y].Name);

                                                                            }
                                                                        }
                                                                    }
                                                                }


                                                                <div id="@i" class="panel-collapse collapse">
                                                                    <div class="box-body">
                                                                        <ul>
                                                                            @foreach (var item2 in ListExperiencias)
                                                                            {
                                                                                <li> <div>@item2</div></li>

                                                                            }
                                                                        </ul>
                                                                    </div>
                                                                </div>

                                                            </div>
                                                            ListExperiencias.Clear();
                                                        }

                                                        if (ViewBag.TituloExperiencias == "ESCUTA, FALA, PENSAMENTO E IMAGINAÇÃO")
                                                        {

                                                            <div class="panel ">
                                                                <div class="box-header ">
                                                                    <h4 class="box-title">
                                                                        <a data-toggle="collapse" data-parent="#accordion" href="#@i" style="color:black">
                                                                            @ViewBag.TituloExperiencias
                                                                        </a>
                                                                    </h4>
                                                                </div>


                                                                @for (int y = 0; y < Model.ExperienciasFormulariosVM.Count; y++)
                                                                {

                                                                    if (Model.ExperienciasFormulariosVM[y].FormularioID == item.FormularioID)
                                                                    {
                                                                        if (Model.ExperienciasFormulariosVM[y].Checked)
                                                                        {

                                                                            if (Model.ExperienciasFormulariosVM[y].CheckBoxID >= 15 & Model.ExperienciasFormulariosVM[y].CheckBoxID <= 23)
                                                                            {
                                                                                ListExperiencias.Add(@Model.ExperienciasFormulariosVM[y].Name);

                                                                            }
                                                                        }
                                                                    }
                                                                }


                                                                <div id="@i" class="panel-collapse collapse">
                                                                    <div class="box-body">
                                                                        <ul>
                                                                            @foreach (var item2 in ListExperiencias)
                                                                            {
                                                                                <li> <div>@item2</div></li>

                                                                            }
                                                                        </ul>
                                                                    </div>
                                                                </div>

                                                            </div>
                                                            ListExperiencias.Clear();
                                                        }
                                                        if (ViewBag.TituloExperiencias == "ESPAÇOS, TEMPOS, QUANTIDADES")
                                                        {

                                                            <div class="panel ">
                                                                <div class="box-header ">
                                                                    <h4 class="box-title">
                                                                        <a data-toggle="collapse" data-parent="#accordion" href="#@i" style="color:black">
                                                                            @ViewBag.TituloExperiencias
                                                                        </a>
                                                                    </h4>
                                                                </div>


                                                                @for (int y = 0; y < Model.ExperienciasFormulariosVM.Count; y++)
                                                                {

                                                                    if (Model.ExperienciasFormulariosVM[y].FormularioID == item.FormularioID)
                                                                    {
                                                                        if (Model.ExperienciasFormulariosVM[y].Checked)
                                                                        {
                                                                            if (Model.ExperienciasFormulariosVM[y].CheckBoxID >= 24 & Model.ExperienciasFormulariosVM[y].CheckBoxID <= 33)
                                                                            {
                                                                                ListExperiencias.Add(@Model.ExperienciasFormulariosVM[y].Name);

                                                                            }
                                                                        }
                                                                    }
                                                                }


                                                                <div id="@i" class="panel-collapse collapse">
                                                                    <div class="box-body">
                                                                        <ul>
                                                                            @foreach (var item2 in ListExperiencias)
                                                                            {
                                                                                <li> <div>@item2</div></li>

                                                                            }
                                                                        </ul>
                                                                    </div>
                                                                </div>

                                                            </div>
                                                            ListExperiencias.Clear();
                                                        }


                                                    }

                                                }
                                            }


                                        </div>
                                    </div>
                                    <!-- /.box-body -->
                                </div>
                                <!-- /.box -->
                            </div>
                            <!-- /.col -->
                        </div>
                    </div>
                    <!-- /.box-body -->

                    @if (Model.FormIdComoFoiAExper.Count != 0)
                    {
                        var count = 0;
                        foreach (var f in Model.FormIdComoFoiAExper)
                        {
                            if (Model.FormIdComoFoiAExper[count] == item.FormularioID)
                            {
                                countIdFormsDeComoFoi2 = count;
                            }

                            count++;
                        }

                        if (!(Model.FormIdComoFoiAExper[countIdFormsDeComoFoi2] == item.FormularioID))
                        {

                            DateTime NowTime = DateTime.Now;
                            var maniData = item.Data;
                            var DataExpirada = maniData.AddDays(7);

                            int result = DateTime.Compare(maniData, NowTime);
                            ViewBag.MensagemDeExpirado = "Já faz mais de uma semana que você criou o plano de aula, gostaria de informar algum Insight?";

                            //mudar a classe para mudar a cor e a menssagem caso tenha expirado a data
                            var mudaClasse = "";
                            var condicaoMudarClasse = false;
                            var perguntaSobreOInsight = "";

                            if (NowTime < DataExpirada)
                            {
                                condicaoMudarClasse = true;
                                mudaClasse = "bg-teal active";
                                perguntaSobreOInsight = "Surgiu Insight?";

                            }
                            else
                            {
                                mudaClasse = "bg-danger #f56954 ";
                                perguntaSobreOInsight = "Já passou uma semana desde a criação deste plano de aula, Surgiu Insight?";
                            }
                            <div class="box-footer" style="color:rebeccapurple">
                                <div class="@mudaClasse color-palette insightOculto" style="display:block; padding:10px" id="divInsights">
                                    <p>@perguntaSobreOInsight</p>
                                    @{
                                        ViewBag.testeForm = item.FormulariosID;
                                        int pegaForm = ViewBag.testeForm;
                                    }

                                    <button type="submit" class="btn escondeInsight botao-insights-sim" id="ClickInsightSim" data-toggle="collapse" data-target="#@item.FormularioID" aria-expanded="false" aria-controls="collapseExample" name="decisao" value="@item.FormularioID" onclick="ClickInsightSim(@item.FormularioID)">Sim</button>

                                    <button type="submit" class="btn insightButtonNao botao-insights-nao" id="ClickInsightNao" data-toggle="modal" name="decisao" value="@item.FormularioID" onclick="ClickInsightNao(@item.FormularioID)">@Html.ActionLink("Não", actionName: "Create", controllerName: "ComoFoiAsExperiencias", routeValues: new { idForm = item.FormularioID, idUsuario = item.UsuarioId }, htmlAttributes: new { @style = "color:white" })</button>
                                    <p> </p>
                                    <!--Sim-->
                                    <div class="collapse" id="@item.FormularioID">
                                        <div class="card card-body">
                                            O insight mudou o padrão do que você tinha planejado?

                                            <button type="button" class="btn botao-insights-sim" data-toggle="modal" data-target="#modellPopUpModifiqueSim" style="color:black">Sim</button>
                                            <button type="submit" class="btn insightButtonNao escondeInsight botao-insights-nao" id="ClickInsightNao" data-toggle="modal" name="decisao" value="@item.FormularioID" onclick="ClickInsightNao()">@Html.ActionLink("Não", actionName: "Create", controllerName: "ComoFoiAsExperiencias", routeValues: new { idForm = item.FormularioID, idUsuario = item.UsuarioId }, htmlAttributes: new { @style = "color:white" })</button>
                                        </div>
                                    </div>
                                </div>
                            </div>
                            <!-- /.box-footer-->

                            countIdFormsDeComoFoi2++;

                            //essa avriável vai para a chave estrangeira do insight
                            var recebeFormularioID = item.FormularioID;

                        }
                        else
                        {
                            <tr style="border:hidden">
                                <td colspan="5" class="tdDoInsight">
                                    <div class="alert alert-success" role="alert">
                                        Você já informou a experiência desse planejmento! Para fechar


                                        <button type="submit" class="btn-md btn-link" value="@item.FormularioID" style="margin-bottom:4px; margin-left:-10px"><b class="alert-success"> @Html.ActionLink("Clique aqui", "Delete", new { id = item.FormularioID })</b></button>
                                        @*<button type="submit" class="btn-md btn-link" value="@item.FormularioID" onclick="OcultaForm(@item.FormularioID)" style="margin-bottom:4px; margin-left:-10px"><b class="alert-success">clique aqui!</b></button>*@
                                    </div>
                                </td>
                            </tr>

                            if (Model.FormIdComoFoiAExper.Count != 1)
                            {

                                if (countIdFormsDeComoFoi2 < Model.FormIdComoFoiAExper.Count - 1)
                                {
                                    countIdFormsDeComoFoi2++;
                                }



                            }

                        }
                    }
                    else
                    {


                        DateTime NowTime = DateTime.Now;
                        var maniData = item.Data;
                        var DataExpirada = maniData.AddDays(7);

                        int result = DateTime.Compare(maniData, NowTime);
                        ViewBag.MensagemDeExpirado = "Já faz mais de uma semana que você criou o plano de aula, gostaria de informar algum Insight?";

                        //mudar a classe para mudar a cor e a menssagem caso tenha expirado a data
                        var mudaClasse = "";
                        var condicaoMudarClasse = false;
                        var perguntaSobreOInsight = "";

                        if (NowTime < DataExpirada)
                        {
                            condicaoMudarClasse = true;
                            mudaClasse = "bg-teal active ";
                            perguntaSobreOInsight = "Surgiu Insight?";

                        }
                        else
                        {
                            mudaClasse = "bg-danger #f56954";
                            perguntaSobreOInsight = "Já passou uma semana desde a criação deste plano de aula, Surgiu Insight?";
                        }
                        <div class="box-footer" style="color:rebeccapurple">
                            <div class="@mudaClasse insightOculto" style="display:block; padding:10px" id="divInsights">
                                <p>@perguntaSobreOInsight</p>
                                @{
                                    ViewBag.testeForm = item.FormulariosID;
                                    int pegaForm = ViewBag.testeForm;
                                }

                                <button type="submit" class="btn escondeInsight botao-insights-sim" id="ClickInsightSim" data-toggle="collapse" data-target="#@item.FormularioID" aria-expanded="false" aria-controls="collapseExample" name="decisao" value="@item.FormularioID" onclick="ClickInsightSim(@item.FormularioID)">Sim</button>

                                <button type="submit" class="btn insightButtonNao botao-insights-nao" id="ClickInsightNao" data-toggle="modal" name="decisao" value="@item.FormularioID" onclick="ClickInsightNao(@item.FormularioID)">@Html.ActionLink("Não", actionName: "Create", controllerName: "ComoFoiAsExperiencias", routeValues: new { idForm = item.FormularioID, idUsuario = item.UsuarioId }, htmlAttributes: new { @style = "color:white" })</button>
                                <p> </p>
                                <!--Sim-->
                                <div class="collapse" id="@item.FormularioID">
                                    <div class="card card-body">
                                        O insight mudou o padrão do que você tinha planejado?

                                        <button type="button" class="btn botao-insights-sim" data-toggle="modal" data-target="#modellPopUpModifiqueSim" style="color:black">Sim</button>
                                        <button type="submit" class="btn insightButtonNao escondeInsight botao-insights-nao" id="ClickInsightNao" data-toggle="modal" name="decisao" value="@item.FormularioID" onclick="ClickInsightNao()">@Html.ActionLink("Não", actionName: "Create", controllerName: "ComoFoiAsExperiencias", routeValues: new { idForm = item.FormularioID, idUsuario = item.UsuarioId }, htmlAttributes: new { @style = "color:white" })</button>
                                    </div>
                                </div>
                            </div>
                        </div>
                        <!-- /.box-footer-->
                        if (countIdFormsDeComoFoi2 < Model.FormIdComoFoiAExper.Count - 1)
                        {
                            countIdFormsDeComoFoi2++;
                        }

                        //essa avriável vai para a chave estrangeira do insight
                        var recebeFormularioID = item.FormularioID;
                    }

                </div>
                    <!-- /.box -->

                    <div class="modal fade" id="modellPopUpModifiqueSim">
                        <div class="modal-dialog">
                            <div class="modal-content">
                                <div class="modal-header">
                                    <button type="button" class="close" data-dismiss="modal" aria-label="Close">
                                        <span aria-hidden="true">&times;</span>
                                    </button>
                                    <h4 class="modal-title">Atenção</h4>
                                </div>
                                <div class="modal-body">
                                    <p>Você irá modificar os campos de objetivo!</p>
                                </div>
                                <div class="modal-footer">
                                    <button type="button" class="btn btn-default pull-left" data-dismiss="modal">Fechar</button>
                                    <button type="submit" class="btn btn-primary escondeInsight" value="3104"><a href="/Formularios/EditarModificacaoDePadrao" style="color:white">Ok</a></button>
                                </div>
                            </div>
                            <!-- /.modal-content -->
                        </div>
                        <!-- /.modal-dialog -->
                    </div>
                    contFormulario++;
                }
            }



        }
    </section>

    <!-- /.content -->
</div>
<!-- /.content-wrapper -->



<script>

                    $(document).ready(function () {

                        //verifica quais botões foream clicados insights
                        $(".insightButton").click(function () {
                            $(".insightButtonNao").hide(); document.getElementById("ClickInsightNao").style.display = "none";
                        });

                    });
                        //envia para o controller
                        function ClickInsightSim(formularioID) {

                            $.ajax({
                                url: '@Url.Action("InsertInsight", "Insights")',
                                type: 'POST',
                                data: { movieName: "Sim", formularioId: formularioID }
                            });


                    }

                        function ClickInsightNao(formularioID) {

                            $.ajax({
                                url: '@Url.Action("InsertInsight", "Insights")',
                                type: 'POST',
                                data: { movieName: "Nao", formularioId: formularioID }
                            });

                            $.ajax({
                                url: '@Url.Action("ComoFoiAsExperiencias", "Create")',
                                type: 'POST',
                                data: { formularioId: formularioID }


                            })
                    }

                        //envia para o controller
                    function OcultaForm(ocultaFormID) {

                        alert(ocultaFormID);
                            $.ajax({
                                url: '@Url.Action("Formularios", "Index")',
                                type: 'POST',
                                data: { ocultaFormID: ocultaFormID }
                            });


                        }
</script>